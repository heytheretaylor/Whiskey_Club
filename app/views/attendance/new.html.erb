<%= @session_id %>
<%= @current_attend %>
<% b = @session.date_of %>
<h3>The next session will be on <%= "#{b.strftime("%A")}, the #{b.strftime("%d")} of #{b.strftime("%B")}" %>

<br>

<% if @current_attend == false %>
	Will you be joining us?	
<%else%>
	<%if @current_attend.is_coming %>
		Still coming?
		<% else %>
		Changed your mind?
	<% end %>
<%end%>
<%= render 'attendance/new', attendance: @attendance, current_attend: @current_attend %>


<br>
<%= @can_use %>

<!-- 
<%= form_for(@attendance, :url => attendance_index_path) do |f| %>
<% if @check %>
<%= f.submit "Can't Make it anymore" %>
<% else %>
<%= f.submit "Check-In!" %>
<% end %>
<% end %> -->